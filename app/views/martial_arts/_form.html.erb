<%= form_for(@martial_art) do |f| %>
  <% if @martial_art.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@martial_art.errors.count, "error") %> prohibited this martial_art from being saved:</h2>

      <ul>
      <% @martial_art.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit 'Cadastrar' %>
  </div>
<% end %>
